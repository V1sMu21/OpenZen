# Session Transcript

**Turns:** 1

**Exit:** EXITED

---



通常在 `~/.cargo/bin/opencode`（通过 cargo install 安装）、`/opt/homebrew/bin/opencode`（Homebrew 公式）或 `/usr/local/bin/opencode`（Intel Mac Homebrew / manual install）。

终端跑 `which opencode` 或 `type -a opencode` 可确认当前实际路径。