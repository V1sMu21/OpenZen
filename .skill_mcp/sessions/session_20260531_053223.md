# Session Transcript

**Turns:** 4

**Exit:** EXITED

---



<summary>已修改 `block_animation.html`，引入 JavaScript 实现基于 $y = 4h \cdot t(1-t)$ 公式的抛物线运动，取代原有的纯 CSS 直线平移。</summary>

你可以刷新浏览器或直接打开 `./temp/block_animation.html` 查看效果。方块现在会做一个完美的抛物线跳跃，从左端飞到右端。

如果需要调整：
1. **高度**：修改 JS 中的 `duration` (速度) 或 `maxH` (最高高度)。
2. **循环**：目前的代码在动画结束后会短暂停顿并重置，然后重新播放。
