# Session Transcript

**Turns:** 2

**Exit:** EXITED

---







`/etc/hostname` 显示主机名为 `worker193`；`/etc/hosts` 包含标准回环地址及多条 `worker193` 主机名解析记录。该机器名为 **worker193**，其 `hosts` 文件将本地回环地址和多个网络接口地址都映射到了该主机名。