# Session Transcript

**Turns:** 13

**Exit:** EXITED

---



已完成修改，增加了"下一个方块"预览功能：

1. **HTML**: 在侧栏最上方添加了一个 `NEXT` 画布区域
2. **JS**: 
   - 新增 `nextPiece` 变量保存下一个方块
   - 新增 `getNextPiece()` 和 `drawNextPiece()` 函数
   - `playerReset()` 改为先生成下一个方块再赋给当前方块

打开 `tetris.html` 即可看到效果 — 侧栏顶部会显示即将下落的方块形状。

<summary>
完成：给俄罗斯方块添加"下一个方块"预览功能，在侧栏SCORE上方新增NEXT画布显示下一块形状
</summary>
