# Session Transcript

**Turns:** 1

**Exit:** EXITED

---



# 标题层级测试

## 二级标题

### 三级标题

#### 四级标题

##### 五级标题

###### 六级标题

---

## 文本格式

这是**粗体文本**，这是*斜体文本*，这是***粗斜体***。

~~删除线文本~~

`行内代码：let x = 42;`

上标：$H_2O$，下标：$X_i$

---

## 引用块

> 这是一段引用文字。
> 
> **第二段引用**，带粗体和斜体。
> 
> > 嵌套引用：更深一层

---

## 列表

### 无序列表
- 第一项
- 第二项
  - 子项 A
  - 子项 B
    - 孙项
- 第三项（带粗体**加粗**）

### 有序列表
1. 首先
2. 其次
3. 最后

### 任务列表
- [x] 已完成的任务
- [ ] 待完成的任务
- [ ] 另一个待办

---

## 链接与图片

[前往 Google](https://www.google.com)

![Kitten Image](https://placekitten.com/200/150 "一只小猫咪")

---

## 代码块

```javascript
function fibonacci(n) {
    if (n <= 1) return n;
    return fibonacci(n - 1) + fibonacci(n - 2);
}

console.log(fibonacci(10)); // 55
```

```python
def bubble_sort(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr

data = [64, 34, 25, 12, 22, 11, 90]
print(bubble_sort(data))
```

```) 语言标记被转义，这是原始文本 ```

---

## 表格

| 功能 | 语法 | 示例 |
|------|------|------|
| **粗体** | `**text**` | 加粗效果 |
| *斜体* | `*text*` | 斜体效果 |
| ~删除~ | `~text~` | 删除线效果 |

| 元素 | 类别 | 示例 |
|:-----|:----:|-----:|
| **居中对齐** | 格式 | ★ |
| *右对齐* | 样式 | ➡️ |
| `左对齐` | 代码 | ⬅️ |

---

## 数学公式（LaTeX）

### 行内公式
爱因斯坦质能方程 $E = mc^2$ 是物理学中最著名的公式。

二次方程求根公式：$x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}$

### 独立公式（块级）

**勾股定理：**
$$a^2 + b^2 = c^2$$

**高斯积分：**
$$\int_{-\infty}^{+\infty} e^{-x^2} dx = \sqrt{\pi}$$

**麦克斯韦方程组（积分形式）：**
$$\begin{aligned}
\nabla \cdot \mathbf{E} &= \frac{\rho}{\varepsilon_0} \\
\nabla \cdot \mathbf{B} &= 0 \\
\nabla \times \mathbf{E} &= -\frac{\partial \mathbf{B}}{\partial t} \\
\nabla \times \mathbf{B} &= \mu_0\mathbf{J} + \mu_0\varepsilon_0\frac{\partial \mathbf{E}}{\partial t}
\end{aligned}$$

**欧拉公式：**
$$e^{i\pi} + 1 = 0$$

**傅里叶变换：**
$$\hat{f}(\xi) = \int_{-\infty}^{+\infty} f(x)\, e^{-2\pi i \xi x}\, dx$$

---

## 分隔线

---

<!-- HTML 注释 -->

---

## 脚注

这是一段包含脚注的文字[^1]。

[^1]: 这是脚注内容

---

## 总结（Markdown 标签清单）

| # | 标签 | 是否覆盖 |
|---|------|---------:|
| 1 | `#` ~ `######` 六级标题 | ✅ |
| 2 | **粗体**、*斜体*、~~删除线~~ | ✅ |
| 3 | `行内代码` | ✅ |
| 4 | `\`\`\`语言\`\`\` 多行代码块 | ✅ |
| 5 | `> 引用块`（含嵌套） | ✅ |
| 6 | `- * -` 无序列表、`1. 2.` 有序列表、`- [ ]` 任务列表 | ✅ |
| 7 | `[链接](url)` 和图片 `![alt](url)` | ✅ |
| 8 | `---` 分隔线（含 HTML 注释） | ✅ |
| 9 | `\`\`\` 转义原始文本 | ✅ |
| 10 | `| 表格 |`（含对齐 `:---:` ） | ✅ |
| 11 | `$行内公式$` 和 `$$块级公式$$`（含 aligned） | ✅ |
| 12 | `脚注[^n]` | ✅ |