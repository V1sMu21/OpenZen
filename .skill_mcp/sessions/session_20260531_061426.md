# Session Transcript

**Turns:** 8

**Exit:** EXITED

---



已修复。问题出在这行：

```js
// 修改前（加了 +60，导致底部离横线有 60px 间隙）
box.style.bottom = `${currentY + 60}px`;

// 修改后（去掉 +60，底部紧贴横线）
box.style.bottom = `${currentY}px`;
```

当 `progress = 0`（起点）或 `progress = 1`（终点）时，抛物线公式算出 `currentY = 0`，此时 `bottom: 0px` 正好让方块底部落在横线上。
