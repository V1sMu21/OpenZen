# Session Transcript

**Turns:** 12

**Exit:** EXITED

---








































It seems `opencode` is not installed or I cannot find it. I'll inform the user and provide the standard path.

Actually, the user might be referring to a tool they are developing or a less common one. I'll ask them directly, but also provide the template for `uraimo/opencode` as it's the most popular one.

The prompt says "find opencode's config file". I have to say I couldn't find it and tell them the standard location.

Wait, maybe "opencode" is a typo for something else? Or maybe it's installed in `./temp` which is currently empty.

I will provide the standard location and how to configure it for Agnes (likely a custom endpoint).

**Standard opencode (uraimo/opencode) Config:**
File: `~/.config/opencode/config.yaml`

Example content for a custom model:
```yaml
provider: openai
config:
  api_key: "your-agnes-api-key"
  model_name: "agnes-model-name"
  base_url: "https://your-agnes-api-url/v1"
```

I'll explain this to the user.