# Session Transcript

**Turns:** 4

**Exit:** EXITED

---


好的，现在我有了完整的代码。我需要添加"下一个方块"的预览功能。

主要修改点：
1. 在 HTML 中添加一个用于显示下一个方块的 canvas 元素
2. 添加 CSS 样式
3. 在 JavaScript 中添加：
   - nextPiece 变量存储下一个方块
   - drawNextPiece() 函数绘制下一个方块预览
   - playerReset() 中生成下一个方块
   - 更新 updateScore() 或相关函数

让我来修改代码：

1. HTML部分：在 sidebar 中添加下一个方块的显示区域
2. CSS：添加样式
3. JS：
   - 添加 nextPiece 变量
   - 修改 playerReset() 生成下一个方块
   - 添加 drawNextPiece() 函数
   - 在 game loop 或相关位置调用绘制

让我修改代码。
</prompt>

好的，我找到了你的俄罗斯方块代码。现在我来添加"下一个方块"预览功能。需要：
1. 在侧边栏添加预览画布
2. 修改 JS 逻辑来生成和绘制下一个方块
