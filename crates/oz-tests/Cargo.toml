[package]
name = "oz-tests"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]

# D3 (round3): memory-recall evaluation harness runs as an explicit,
# ignored test — dev-only so release builds never link the engine twice.
[dev-dependencies]
entropy_memory_engine = { path = "../../vendor/entropy-memory-engine" }
tempfile = "3"
