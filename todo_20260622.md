# OpenZen 会话持久化 & 上下文压缩 — 2026-06-21

## 已完成修复

### 1. 会话重启后历史丢失 (lib.rs)
- **根因**: Tauri 后端在 `send_message()` 和 `run_agent_for_session()` 中写入消息后从未调用 `store.save()`，消息仅存内存。
- **修复**: 两处各加 1 行 `store.save()`，对齐 web server 模式的 `mod.rs:608/1205`。
- **文件**: `src-tauri/src/lib.rs` L276、L760。

### 2. 上下文条统计不准确 (agent_loop.rs + 前端)
- **根因 1**: `total_input_tokens = usage.input_tokens` (覆盖) → 应 `+=` (累加)。
- **根因 2**: 上下文条用 `sum(messages[].tokensIn)` 求和，每次 LLM 调用独立计费，求和=重复计数。
- **根因 3**: Tauri build 只编译了 Rust 但没 `npm run build` 前端 → 嵌入的是旧前端。
- **修复**:
  - `agent_loop.rs:511-512` `=` → `+=`
  - 新增 `last_turn_input_tokens` 字段（每轮 LLM 调用的实际上下文大小）
  - 新增 `contextTokens` 字段贯穿 LoopOutcome → SseEvent::done() → 前端消息
  - 上下文条改用最后一条消息的 `contextTokens`，不再对所有消息求和
- **文件**: `agent_loop.rs`, `sse_bus.rs`, `lib.rs`, `mod.rs`, `types.ts`, `chat.ts`, `App.svelte`, `ContextBar.svelte`

### 3. 自动压缩删掉 system prompt 导致 llm_error (compress.rs)
- **根因**: `compress_messages` Phase 2 无条件 `messages.remove(0)` 删掉第一条消息=system prompt。
- **修复**: 改为 `sys_offset` 跳过所有 System 角色消息，从第一个非 System 消息开始删除。
- **文件**: `compress.rs` L103-121

### 4. /compact 命令显示 0 chars (lib.rs + mod.rs)
- **根因**: `serde_json::from_value::<ga_core_types::Message>()` 反序列化 fail — 存盘格式是扁平 JSON（content 是 string），但 Message::content 是 Vec<ContentBlock>。
- **修复**: 改为手动从 JSON 字段构造 Message，与 `run_agent_for_session` 中读取历史同逻辑。
- **文件**: `lib.rs` L386-400, `mod.rs` L1462-1475

---

## 待解决

### A. /compact 重序列化破坏会话数据 (急切)
- **现状**: 修复了"deserialize→compress"方向，但"serialize back"方向仍在用 `serde_json::to_value(&Message)`，会把 content 从 string 变成 `[{text:{text:"..."}}]`，丢掉 tokensIn/tokensOut/timestamp/streamEvents 等所有字段。
- **方案**: Schema B (配对删除，不改原始 JSON)。从后往前匹配压缩后剩余的 Message 与原始 JSON，直接保留对应的 JSON 条目，零序列化。
- **关联**: 已有会话被旧版 /compact 破坏（content 变成 Vec<ContentBlock> 格式，tokensIn=None），需提供修复或清理方式。

### B. 压缩摘要生成机制 (重要)
- **自动压缩** (agent loop 内): 从"直接删"升级为"LLM 摘要→注入→删"。有 LLM client 可用。
- **/compact** (同步命令): 模板驱动提取（元数据+结构化字段），无 LLM，但有度量验证。
- **两个都需实现的组件**:
  - `build_compression_summary()` — 摘要生成
  - `CompressionMetrics` — 量化验证 (Δ≥85%, BR≥0.85, R_A≥0.95, R_B≥0.90)
- **参考**: OpenCode 的 `summarize-retry-strategy` + `compaction-context-injector` + `dynamic-context-pruning`。

### C. Tauri 构建流程 (维护项)
- **注意**: `cargo tauri build` 嵌入 `frontends/dist/` 预编译前端，改完 Svelte 后必须先 `npm run build` 再 `cargo tauri build`。

### D. 已破坏会话的修复
- 已被旧版 /compact 破坏的会话（content=Vec<ContentBlock> 格式、元数据全丢）可能需要手动修复或被新版代码自动容错处理。

---

## 文件改动清单 (本次)

| 文件 | 改动内容 |
|---|---|
| `src-tauri/src/lib.rs` | +save()×2, +contextTokens 存储, compress 反序列化修复 |
| `crates/ga-core/src/agent_loop.rs` | = → +=, +last_turn_input_tokens, +context_tokens_est |
| `crates/ga-core/src/compress.rs` | Phase 2 sys_offset 保护 |
| `crates/ga-server/src/webui/sse_bus.rs` | SseEvent::done() +context_tokens 参数 |
| `crates/ga-server/src/webui/mod.rs` | SseEvent::done() 调用更新, compress 反序列化修复, +contextTokens 存储 |
| `frontends/src/lib/stores/types.ts` | +contextTokens 字段 |
| `frontends/src/lib/stores/chat.ts` | loadSession +contextTokens, finalizeAssistantMessage +contextTokens, done handler +contextTokens |
| `frontends/src/App.svelte` | ctxTokens 改用 last.contextTokens, 不再求和 |
| `frontends/src/lib/components/ContextBar.svelte` | 同上 |

---

## 上下文统计问题的完整说明

### 两个 token 指标的本质不同

| 指标 | 含义 | 数据流 | 显示位置 |
|---|---|---|---|
| `tokensIn` (per message) | 该 agent run 消耗的总输入 token（累计所有 LLM turn） | agent_loop `total_input_tokens` (+= 累加) → LoopOutcome → 消息存储 | Footer "IN" |
| `contextTokens` (per message) | 该 agent run 最后一轮 LLM 调用的上下文大小 | agent_loop `last_turn_input_tokens` (= 每次覆盖) → LoopOutcome → 消息存储 | 上下文条 "xx/200K" |

**两者来源相同（LLM API 的 `usage.input_tokens`），但含义不同**：
- `tokensIn` = 成本视角：这轮回复一共消耗了多少输入 token（对 API 计费有意义）
- `contextTokens` = 容量视角：当前上下文装了多少东西（对"会不会超窗口"有意义）

### 当前 Footer 显示逻辑

```
[OUT: 累计] [IN: 累计] [最后轮次上下文/200K ████ Context]
              ↑                    ↑
        所有消息 tokensIn 的和   最后消息的 contextTokens
             （成本视角）            （容量视角）
```

Footer 的 "IN" = `sum(messages[].tokensIn)`（所有 agent run 的累计成本），上下文条 "xx" = `last.contextTokens`（最后一轮 LLM 调用的上下文大小）。两者是完全不同的数值。`contextTokens` 通常远小于 `sum(tokensIn)`，因为每次工具调用都是一个完整的上下文发送（含全部历史），但上下文条只取最后一次。

**用户困惑点**：看到 IN=355K 就觉得上下文满了 355K，但实际上 355K 是累计成本（20 轮 LLM 调用 × 每轮 15-20K 输入），当前上下文实际只有 ~20K。

### 当前状态：基础修复已完成

- ✅ agent_loop 中 `=` → `+=` 修复（正确累加成本）
- ✅ `contextTokens` 字段分离（正确捕获上下文大小）
- ✅ 上下文条改用 `contextTokens`（不再求和 tokensIn）
- ✅ 前端已重建嵌入 Tauri

### 待改进：Footer 信息架构混乱（低优先级）

Footer 同时显示两个不同维度的数字但标签相同（都是 "IN"），易混淆。可能的改进方向：
- Footer "IN" 改为显示 `last.contextTokens`（当前上下文），与上下文条一致
- 或保留 Footer "IN" 为累计成本，但上下文条改标签为 "Used/Total" 而非显示 IN 的值
- 或在 Footer 加第三个数字区分"本轮上下文用量"和"累计 token 消耗"

当前不影响功能正确性，可后续决策。
